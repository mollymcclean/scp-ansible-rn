[Unit]
Description=Consul Startup process
After=network.target

[Service]
User=consul
Group=consul
Type=simple
ExecStart=/bin/bash -c '/usr/local/bin/consul agent -config-dir {{ consul_common_dir }}/'
TimeoutStartSec=0

[Install]
WantedBy=default.target
