[Unit]
Description=consul-template

[Service]
ExecStart=/bin/sh -c "/usr/local/bin/consul-template  -config={{ consul_tpl_home }}/config >>  {{ consul_log_file }}/{{ consul_tpl_name_service }}.log 2>&1"

Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
